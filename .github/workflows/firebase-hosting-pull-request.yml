# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write

env: # <--- here
  VITE_API_KEY: ${{secrets.VITE_API_KEY}}
  VITE_AUTH_DOMAIN: ${{secrets.VITE_AUTH_DOMAIN}}
  VITE_PROJECT_ID: ${{secrets.VITE_PROJECT_ID}}
  VITE_STORAGE_BUCKET: ${{secrets.VITE_STORAGE_BUCKET}}
  VITE_MESSAGING_SENDER_ID: ${{secrets.VITE_MESSAGING_SENDER_ID}}
  VITE_APP_ID: ${{secrets.VITE_APP_ID}}
  VITE_MEASUREMENT_ID: ${{secrets.VITE_MEASUREMENT_ID}}
  GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - name: Imprmir variables de entorno
        run: echo ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }} && echo ${{ secrets.FIREBASE_SERVICE_ACCOUNT_LEONARDO_TRUJILLO }}

      - uses: actions/checkout@v3
      - run: npm install && echo '-----Install Done 👌-----' && npm run build && echo '-----Build Done 👌-----'
      
      # - name: Setup Node
      #   uses: actions/setup-node@v3

      # - name: Install dependencies
      #   uses: bahmutov/npm-install@v1

      # - name: Build project
      #   run: npm run build
      # - uses: actions/checkout@v4
      # - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_LEONARDO_TRUJILLO }}
          projectId: leonardo-trujillo
